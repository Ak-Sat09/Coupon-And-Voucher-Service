spring:
  application:
    name: CouponVoucherService

  datasource:
    url: jdbc:mysql://localhost:3306/pay
    username: root
    password: Mehla@6228
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect